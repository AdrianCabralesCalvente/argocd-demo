apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-msg.fullname" . }}
  labels:
    {{- include "hello-msg.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hello-msg.selectorLabels" . | nindent 4 }}
