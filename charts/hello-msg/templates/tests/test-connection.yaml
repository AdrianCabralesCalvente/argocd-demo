apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hello-msg.fullname" . }}-test-connection"
  labels:
    {{- include "hello-msg.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hello-msg.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
